import { AbstractFabricService } from './abstract-fabric-service';

describe('AbstractFabricService', () => {
  it('should create an instance', () => {
    expect(new AbstractFabricService()).toBeTruthy();
  });
});
